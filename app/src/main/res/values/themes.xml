<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PressureDiary" parent="Theme.MaterialComponents.DayNight">
        <!-- Primary brand color. -->
        <item name="colorPrimary">#A9ECE5</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:textColorPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <!-- This attributes is used by title -->

        <!-- Область данных измерений -->
        <item name="measurement_value">@color/light_green</item>
        <item name="measurement_frame">@color/orange</item>
        <item name="measurement_second_plan">@color/white</item>

        <!-- Заголовок времени главный экран -->
        <item name="time_background">@color/white_grey</item>

        <!-- значения по умолчанию -->
        <item name="default_text">@color/black</item>

    </style>
</resources>