<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PressureDiary" parent="Theme.MaterialComponents">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>
        <item name="android:textColorPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <!-- Customize your theme here. -->

        <!-- Область данных измерений -->
        <item name="measurement_value">@color/white</item>
        <item name="measurement_frame">@color/white</item>
        <item name="measurement_second_plan">@color/black</item>

        <!-- Заголовок времени главный экран -->
        <item name="time_background">@color/white_grey_diary</item>

        <!-- значения по умолчанию -->
        <item name="default_text">@color/white</item>
    </style>
</resources>